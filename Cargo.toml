[package]
name = "goose"
version = "0.2.0"
edition = "2021"
rust-version = "1.70"
description = "Step-by-step install helper. Tells you what to type, doesn't run it."

[[bin]]
name = "goose"
path = "src/main.rs"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
